#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void decode() {
	int n, m;
	cin >> n >> m;
	cout << ((n + 1) / 2) * ((m + 1) / 2) << "\n";
}
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	decode();
	return 0;
}
